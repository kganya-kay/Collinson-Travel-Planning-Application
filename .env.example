# GraphQL Server Configuration
PORT=4000
NODE_ENV=development

# OpenMeteo API Configuration
# These are public endpoints (no API key required)
# You can override these for custom proxy or mirror endpoints
OPENMETEO_GEOCODING_URL=https://geocoding-api.open-meteo.com
OPENMETEO_FORECAST_URL=https://api.open-meteo.com
